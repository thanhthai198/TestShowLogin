import {StyleSheet} from 'react-native'

export default StyleSheet.create({
    root: {
        flex: 1,
        alignItems: 'center',
        backgroundColor: '#fff',
        justifyContent: 'center',
    },
});
